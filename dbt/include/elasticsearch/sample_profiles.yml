
default:
  outputs:
    dev:
      type: elasticsearch
      # Add sample credentials here, like:
      # host: <host>
      # port: <port_num>
      # api_key_id: <user>
      # api_key_secret: <pass>
  target: dev
